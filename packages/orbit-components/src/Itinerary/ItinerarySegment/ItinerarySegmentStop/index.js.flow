// @flow
import * as React from "react";

export type Props = {|
  +date: string,
  +time: string,
  +city: string,
  +station: string,
  +hidden?: boolean,
  +warning?: boolean,
  +minWidth?: number,
|};

declare export default React.ComponentType<Props>;
