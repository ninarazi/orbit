// @flow
import * as React from "react";

export type Context = {|
  +isNextHidden: boolean,
  +noElevation: boolean,
  +isHidden: boolean,
  +index: number,
  +opened: boolean,
  +setOpened: () => void,
  +last: boolean,
  +count: number,
|};

export type ProviderProps = {|
  +children: React.Node,
  +noElevation: boolean,
  +isNextHidden: boolean,
  +isHidden: boolean,
  +index: number,
  +opened: boolean,
  +setOpened: () => void,
  +last: boolean,
  +count: number,
|};

declare export var ItinerarySegmentProvider: React.ComponentType<ProviderProps>;
declare export function usePart(): Context;
