// @flow
import type { Tokens } from "@kiwicom/orbit-design-tokens/lib/index";

export type Theme = {|
  +orbit: Tokens,
  +transitions?: boolean,
  +rtl?: boolean,
|};

export type ThemeProps = {|
  +theme: Theme,
|};

declare var defaultTheme: Theme;

export default defaultTheme;
